import 'package:flutter/material.dart';

class WorldStatesView extends StatelessWidget {
  const WorldStatesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
